import Layout from "../Layout";
import { PageNotFound } from "../PageNotFound";

export default function NotFoundPage(){
    return (
        <Layout>
            <PageNotFound />
        </Layout>
    )
}
